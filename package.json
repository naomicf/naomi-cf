{
  "name": "gatsby-starter-default",
  "description": "Gatsby default starter",
  "version": "1.0.0",
  "author": "Kyle Mathews <mathews.kyle@gmail.com>",
  "dependencies": {
    "gatsby": "^1.9.241",
    "gatsby-link": "^1.6.39",
    "gatsby-plugin-react-helmet": "^2.0.8",
    "react-helmet": "^5.2.0"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "scripts": {
    "develop": "gatsby develop -p 8680 -H 0.0.0.0",
    "build": "gatsby build",
    "format": "prettier --write 'src/**/*.js'",
    "serve": "gatsby serve",
    "test": "echo \"Error: no test specified\" && exit 1",
    "firebase-serve": "node_modules/.bin/firebase serve -p 8680 -o 0.0.0.0",
    "deploy": "node_modules/.bin/firebase deploy"
    
  },
  "devDependencies": {
    "prettier": "^1.11.1",
    "firebase-tools": "^3.17.7"
  }
}
